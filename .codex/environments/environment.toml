# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "Carve"

[setup]
script = '''
echo "[setup] start: $(date) pwd=$(pwd)"

# GitHub token for gh
if [ -f "$HOME/.codex/secrets/github_token" ]; then
  export GH_TOKEN="$(cat "$HOME/.codex/secrets/github_token")"
  echo "[setup] GH_TOKEN loaded"
else
  echo "[setup] GH_TOKEN file missing: $HOME/.codex/secrets/github_token"
fi

# Enable network for sandboxed runs
mkdir -p .codex
cat > .codex/config.toml <<'EOF'
[sandbox_workspace_write]
network_access = true
EOF
echo "[setup] wrote .codex/config.toml"
ls -la .codex || true
cat .codex/config.toml || true

# Tooling (best effort)
mise trust || true
tuist install || true

echo "[setup] done"
'''
